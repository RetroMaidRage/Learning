unsigned long Perimeter(int length, int width) {
    // Формула для обчислення периметра прямокутника: 2 * (довжина + ширина)
    return 2UL * (static_cast<unsigned long>(length) + static_cast<unsigned long>(width));
}
